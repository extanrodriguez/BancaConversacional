apiVersion: v1
kind: Service
metadata:
  name: banca-conversacional-api
  labels:
    app: banca-conversacional-api
spec:
  selector:
    app: banca-conversacional-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
